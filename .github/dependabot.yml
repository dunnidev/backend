version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "03:00"
    open-pull-requests-limit: 10
    reviewers:
      - "0x860"
    allow: 
      - dependency-type: "direct"
      - dependency-type: "indirect"
    groups:
      security:
        patterns:
          - "*security*"
          - "*cve*"
        # No dependency-types restriction: a security patch of a production
        # dependency (stellar-sdk, express, grpc-js, …) is usually higher
        # priority than a dev-only one and must be grouped here too.
      minor-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
        patterns:
          - "*"
    pull-request-branch-name:
      separator: "/"
    rebase-strategy: "auto"
    commit-message:
      prefix: "chore(deps)"
      prefix-development: "chore(deps-dev)"
      include: "scope"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"